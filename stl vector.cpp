#include<bits/stdc++.h>
using namespace std;

int main()
{
    vector< vector<int> > arr;
    vector<bool> check(10000, false);

    cout<<"Enter how many entries you want to have: ";
    int n; cin>>n;
    int p, q;
    for(int i=0; i<n; i++){
        cin>>p>>q;

    }
}
