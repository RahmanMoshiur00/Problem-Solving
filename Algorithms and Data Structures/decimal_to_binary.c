#include<stdio.h>

int main()
{
    int i, n, m = 1, a;

    scanf("%d", &i);

    for(n = 2; i%2 <= m; ){
       for(a=1; a<=10; a++){
        printf("%d", m);
       }
    }

    return 0;
}
