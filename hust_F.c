/*F*/
#include<math.h>

main()
{
    int i, test;
    double pi = 2 * acos(0.0);
    printf("%.15lf", pi);
    /*double r, area;
    scanf("%d", &test);

    for(i=1; i<=test; i++){
        scanf("%lf", &r);
        if(r<=1000.0){
            area = ((4*r*r) - (pi*r*r)) + pow(10, -9);
            printf("Case %d: %.2lf\n", i, area);
        }
    }*/
}
